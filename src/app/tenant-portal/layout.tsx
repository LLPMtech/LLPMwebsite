export const metadata = {
  title: 'Tenant Portal',
  description: 'Access your Life Long Property Management tenant portal. Log in via Commercial Cafe or Rent Cafe to pay rent, submit maintenance requests, and manage your lease.',
  alternates: { canonical: '/tenant-portal' },
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
